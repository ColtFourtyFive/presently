# Failed publication cleanup checkpoint, September 16, 2026

The integrated suite passed **649 tests across 59 files** in **729.58 seconds**. TypeScript and the production build passed. Release fingerprint `d1ae4da329eaea7e40594492535aedcf4eb5caf53ed354c0996f6e2206a817f3` covers 269 files and remained unchanged during validation. Application version is `0.1.0`; local schema is 27. Migration 27 SHA-256 is `60eb284ae0ed225efbd35a5201a5ab5020e568552e85832b2d965a866c65ec90`.

Schema 27 implements private cleanup of invalid, never-published archive candidates. It retains the original failed build, permanent request ownership, operational attendance data, committed publications, and R2 objects. A replacement publication can claim the released temporary associations using a new publication ID and supported proof.

Three bounded inventory passes verify claims, locators, and part descriptors. The record pass reconstructs the original locator digest. Native leases pin at most eight selected keys. Deletions follow foreign-key order and share a transaction with count checks, the progress update, and a final native assertion. A zero-row checkpoint rolls the page back. Lost replies resolve through the saved revision; replay removes no additional page.

Generation resets pause unfinished work and revoke its lease. An explicit resume must re-inventory all remaining rows before deleting again. Counts and additive hashes must match the original inventory minus completed removals. Tests cover same-count row substitution, missing metadata, maintenance, copied leases, independent encrypted partial recovery, retained committed evidence, and fresh replacement publication. Authority requires the retained admission, rebind, and inventory diagnostics. Native expression-depth failures found during integration were corrected with balanced predicates.

The backup inventory now contains 52 tables; its lock/count/schema/reference snapshot remains four native statements. Frozen-hash empty and populated migration rehearsals passed with rollback, maintenance, source values, logical audit history, accepted receipts, integrity, and foreign keys verified. The populated pre-history-backfill file measures 134,238,208 bytes after local VACUUM. This is local SQLite evidence, not deployed D1 reclamation or resource capacity.

Public archived lookup, historical interval/report/correction authority, addenda, holds, source eviction, scheduler budget completion, and deployed measurements remain unfinished. Cleanup adds no public endpoint or unattended scheduler. No deployment, live mutation, source deletion, backup activation, or paid upgrade occurred. Railway remains available. The last verified Cloudflare installation is still empty on schema 1–4 with export-credential approval and alert setup pending.

Live backup delivery and independent populated cloud restoration, physical iPad/staff/outage acceptance, and Kumon-owned accounts, keys, training, contracts, and handover remain release gates. The full goal remains active.

Evidence: [checkpoint manifest](../tmp/abandonment27-checkpoint-evidence-20260916.json), [cleanup contract](archive-publication-abandonment.md), [empty migration](../tests/empty-native-migration-results-27-final.json), [populated migration](../tests/full-release-migration-results-27-final.json). Prior checkpoint artifact hashes were reverified.
