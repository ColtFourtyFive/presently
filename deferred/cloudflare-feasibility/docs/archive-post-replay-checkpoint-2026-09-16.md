# Archived receipt replay checkpoint, September 16, 2026

The affected regression suite passed **122 tests across 9 files**. TypeScript and the production build passed. Release fingerprint `7f893eddf62e38cf6f946929bf247a709eb502a643696f60fd551976543f2140` covers 272 files and remained unchanged during validation. Local schema remains 27. This was a scoped regression run. The earlier full suite passed 649 tests across 59 files before GET and POST archive integration.

All five existing attendance and visit-correction POST retry lookups now accept the same lazy, read-only archive adapter as event receipt GET. Identical historical requests return their original sealed response before current observation-age, student-status, visit-version, and visit-existence checks. Truly unused IDs still use the existing validation and native SQL acceptance path. New corrections still require a live visit.

Permanent request ownership now suppresses a retained physical audit alias independently of archive configuration. A changed valid payload returns the existing 409 conflict without R2 reads even when the archive flag, bucket, or key is unavailable. An identical accepted request with unavailable or inconsistent evidence returns 503. It cannot be recreated as a new observation or correction. Cross-center and cross-kind requests remain undisclosed, and the resolver still rechecks authority after R2 reads.

The isolated candidate passed 81 tests across four files before integration. Fourteen new POST cases cover exact event/correction/null receipts, inactive or changed profiles, retained audit aliases across five storage configurations, missing/corrupt/unavailable evidence, cross-center and cross-kind IDs, normal unused-ID acceptance, both insert-exception replay paths, and the correction missing-row replay path. Tests compare all protected operational, ownership, and publication rows before and after replay and check for R2 writes. Existing native concurrent acceptance, fingerprint encoding, authentication, kiosk, and authority-change checks remain covered by the regression suite.

The deterministic retry-branch tests use a test-only first-snapshot visibility override to represent an earlier unused-ID read. All later insertion attempts and replay reads execute against native isolated D1/R2. The old-event case temporarily changes only the test worker's post-authentication observation-age clock. This is branch and native-guard coverage, not a deployed concurrency or CPU measurement. No test hook is bundled into the product worker.

No schema, deployment, live mutation, source eviction, scheduler activation, or paid-plan change occurred. Source details remain in D1. Historical range/report completeness, current visit and correction authority, addenda, holds, safe source removal, and scheduler budget completion remain unfinished. Live backup delivery and independent cloud restoration, deployed resource measurements, physical iPad/staff/outage acceptance, and Kumon-owned handover remain release gates. Railway stays available and the goal remains active.

Evidence: [current manifest](../tmp/post-replay-checkpoint-evidence-20260916.json), [GET checkpoint and local I/O measurements](archive-public-reader-checkpoint-2026-09-16.md), [prior full-suite checkpoint](archive-abandonment-checkpoint-2026-09-16.md).
