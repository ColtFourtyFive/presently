# Monthly publication checkpoint — September 16, 2026

The integrated suite passed **578 tests across 52 files in 247.80 seconds**. TypeScript and the production build passed. Release fingerprint `e1abb7bf6b2f8b1f5e60573a93a0420b9b81e0bba8768094d54d217bbee7868c` covers 250 files and remained unchanged during validation. Application version is `0.1.0`; local schema is 25. Migration 25 SHA-256 is `b2b4c43454ee2cd977c5849583e183d7ee58ba03027178cc1ffc0a733b485514`.

Schema 25 adds private monthly publication builds, immutable part/record/request locators, a committed descriptor and generation-specific availability. Publication requires a completed supported v2 semantic run and compares immutable live source evidence again in each candidate transaction. It uses pages of at most eight records and 256 KiB of plaintext, with exact lease/revision and maintenance fences. An in-transaction assertion rolls back a page whose final checkpoint guard no longer matches. Terminal count reconciliation is evaluated only on publication commit.

The optional internal request resolver preserves original compact, object and literal-null receipts. It checks ownership and payload conflicts before R2 and rechecks authority after object reads. Local tests cover live preference, staging cleanup independence, unrelated registry rows, missing/corrupt objects, source mismatch, generation changes, lost replies and native rollback. Public attendance routes do not yet activate this dependency.

Backups now include all 48 tables and pin the union of completed legacy roots and all committed publication roots. Exact duplicates collapse and conflicting identities fail safely. The native lock/count/schema/reference snapshot uses four statements. Independent encrypted SQL-plus-v2 recovery passes after closing the source runtime and omitting detailed attendance source rows. Repeated restore resets retain original proof/index/accounting evidence and mark publication availability unavailable. Re-enabling it requires catalog reconciliation, which is not implemented in this checkpoint.

Empty native and populated local migration rehearsals passed at the frozen migration hash, including forced atomic rollback. They preserve source values, logical audits, accepted receipts and maintenance locks; integrity and foreign-key checks pass. The populated fixture retains 120,940 audits and 120,000 accepted receipts. Its post-migration, pre-history-backfill SQLite file is 134,045,696 bytes after VACUUM, 118,784 bytes above the equivalent schema 24 stage. The earlier 175,226,880-byte schema 24 figure includes history backfill and is a different measurement stage. These are local storage measurements, not deployed CPU or Free-tier capacity evidence.

The six tables remain internal. Invalid builds retain request claims and may block replacement until bounded abandoned-candidate cleanup is implemented. No source removal, scheduled publication, addendum authority, current historical visit/report/correction authority or restored-catalog readiness is established. The next archive work is restored-generation catalog reconciliation and abandoned claim cleanup, followed by report/correction integration and safe eviction.

No remote deployment, migration, upload, live data mutation, backup activation or paid upgrade occurred. The last verified Cloudflare installation remains empty on schema 1–4, automatic backups are disabled and export-credential approval is pending. Railway remains available. Live backup delivery/alerts, populated cloud recovery, deployed measurements, physical iPad/staff/outage acceptance and Kumon-owned handover remain release gates. The full production-readiness goal remains active.

Evidence: [checkpoint manifest](../tmp/publication25-checkpoint-evidence-20260916.json), [publication design and limits](archive-publication.md), [native migration](../tests/empty-native-migration-results-25-final.json), [populated migration](../tests/full-release-migration-results-25-final.json). Earlier schema 19–24 checkpoint artifacts remain intact and the immediate prior checkpoint hashes were reverified.
