# Restored publication reconciliation checkpoint — September 16, 2026

The integrated suite passed **616 tests across 56 files** in **272.66 seconds**. TypeScript and the production build passed. Release fingerprint `8862faf8a7717b1b439d3ebf376adab9779a66320b6af4752f9df2ab60bd7a30` covers 260 files and remained unchanged during validation. Application version is `0.1.0`; local schema is 26. Migration 26 SHA-256 is `afe6a618bb8f335da1a1bdb5efd8c93b70d806908e24f8478016c265c92d6b26`.

Schema 26 adds bounded internal reconciliation jobs and immutable receipts. A fresh current-generation completed semantic proof must match the original publication. Authenticated record pages compare every locator, request claim and permanent owner; separate bounded catalog scans reach EOF and reject extra entries. The terminal transaction atomically records completion and activates availability for exactly that publication. Original proof provenance remains unchanged.

Publication and reconciliation share the frozen locator version 1 serializer, page partition and digest chain. Independent vectors preserve existing hashes across the eight-record, 256 KiB, part, UTF-8 and property-order boundaries. Native query plans use bounded indexed catalog reads; terminal counts stop just beyond supported limits. These establish local code bounds, not deployed quota or CPU safety.

Tests cover missing/extra records, descriptor/row/fingerprint/digest mismatches, native checkpoint and terminal rollback, lost replies, current-generation changes, missing availability, and exact compact/object/null receipt resolution. The API separates retained job `state` from current `ready` availability. After another restore, a retained completed job reports `ready: false`; its receipt does not authorize the new generation.

An independent encrypted recovery drill closes the original runtime, restores SQL with operational source details absent, verifies the downloaded archive graph, reconciles it, and returns original event/correction responses. A second reset preserves original publication and reconciliation evidence while disabling old authority. The backup inventory includes 50 tables, with the lock/count/schema/reference snapshot still four native statements.

Both frozen-hash migration rehearsals passed, including atomic rollback, maintenance, source preservation, integrity and foreign keys. The populated fixture preserves 120,940 logical audits and 120,000 accepted receipts. Its post-migration, pre-history-backfill SQLite file is 134,139,904 bytes after VACUUM, 94,208 bytes above the equivalent schema 25 stage. This is not comparable to the earlier 175,226,880-byte schema 24 result after history backfill without accounting for that different stage.

This activates only the internal, proven recovery path for an individual publication. Public archive lookup, unattended scheduling, account-loss cloud recovery and deployed capacity remain unverified or disabled. Abandoned unpublished claims remain reserved. Next implement bounded abandonment cleanup, then public historical lookup/report/correction authority, addenda/holds and safe source eviction. Live source data remains in D1.

No remote deployment, migration, upload, live data mutation, automatic backup activation or paid upgrade occurred. The last verified live Cloudflare installation remains empty on schema 1–4; export-credential approval and alert setup are pending. Railway remains available. Physical iPad/staff/outage acceptance, customer-owned accounts/key custody and contractual handover remain release gates. The full goal remains active.

Evidence: [checkpoint manifest](../tmp/reconciliation26-checkpoint-evidence-20260916.json), [reconciliation contract](archive-publication-reconciliation.md), [native rehearsal](../tests/empty-native-migration-results-26-final.json), [populated rehearsal](../tests/full-release-migration-results-26-final.json). Earlier checkpoint artifacts remain intact and the prior schema 24/25 manifests were reverified.
